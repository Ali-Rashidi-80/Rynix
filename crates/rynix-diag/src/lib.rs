//! Structured diagnostics for the Rynix compiler.
//!
//! Rynix is AI-native from day one: every diagnostic carries a stable code
//! (`RYX####`), precise spans, and machine-applicable fixes with confidence
//! scores. The JSON renderer implements the `rynix.diag.v1` schema.

pub struct Placeholder;
