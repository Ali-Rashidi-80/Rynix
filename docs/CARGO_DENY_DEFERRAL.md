# Cargo deny — Implemented (Phase 31)

**Status:** Superseded deferral. `deny.toml` + CI job `cargo-deny` (EmbarkStudios
`cargo-deny-action`) enforce advisories/licenses/sources.

Former deferral note retired. Gate: `cargo_deny_clean`.
