# Phase 42 — inlayHint

**Parent:** [GOLDEN_LEAD.md](GOLDEN_LEAD.md)

Gate: `lsp_inlay_hint_smoke` — type hints at `let`/`param` from sema `def_types`.

Contract: `docs/contracts/phase42_inlay.contract.toml`
