{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rynix.dev/schemas/rynix.deps.v1.json",
  "title": "rynix.deps.v1",
  "type": "object",
  "required": ["schema", "manifest", "package", "status", "dependencies"],
  "properties": {
    "schema": { "const": "rynix.deps.v1" },
    "manifest": { "type": "string" },
    "package": { "type": "string" },
    "registry": { "type": ["string", "null"] },
    "workspace": { "type": ["string", "null"] },
    "status": { "type": "string", "enum": ["ok", "error"] },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "kind", "path", "ok", "detail"],
        "properties": {
          "name": { "type": "string" },
          "kind": { "type": "string", "enum": ["path", "registry", "workspace"] },
          "path": { "type": "string" },
          "version": { "type": ["string", "null"] },
          "entry": { "type": ["string", "null"] },
          "sources": {
            "type": "array",
            "items": { "type": "string" }
          },
          "ok": { "type": "boolean" },
          "detail": { "type": "string" }
        }
      }
    },
    "note": { "type": "string" },
    "lock": {
      "type": "object",
      "description": "Optional rynix.lock.toml status (CLI/MCP enrichment)",
      "properties": {
        "present": { "type": "boolean" },
        "ok": { "type": "boolean" },
        "path": { "type": ["string", "null"] },
        "detail": { "type": "string" }
      }
    }
  }
}
