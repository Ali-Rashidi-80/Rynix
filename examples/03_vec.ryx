## Region Vec[i64] methods (mono collections)
def main() -> i64
  let v: Vec[i64] = vec_new(0)
  v.push(10)
  v.push(20)
  v.push(30)
  print_i64(v.len())
  print_i64(v.get(1))
  return v.len() + v.get(0)
end
