## Payload enum i64 match roundtrip (Phase 33 / ADR-0024)

enum Opt
  None
  Some(i64)
end

def main() -> i64
  let a = Some(7)
  let mut n = 0
  match a
    None
      n = 0
    Some(x)
      n = x
    _
      n = -1
  end
  print_i64(n)
  return 0
end
