def main() -> i64
  let n = opaque_i64(2000000)
  let mut i = 0
  let mut acc = 0
  loop
    if i >= n
      break
    end
    acc = acc + i * 3 - i / 2 + i % 7
    i += 1
  end
  print_i64(acc)
  return 0
end
