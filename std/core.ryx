## Soft std prelude (Phase 9+) — names are builtins in the compiler.
## `print`, `sleep_ms`, `yield`, `now_ms`, `fiber_run` lower to `rynix_rt_*`.
## Collections: use `[i64]` arrays with bounds-checked index + BCE.

## Example:
##   let xs = tensor(3, [1, 2, 3])
##   let t = now_ms()
